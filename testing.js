let x = 10 // use let or const instead of var
x += 5 // use += operator instead of reassignment
console.log('The value of x is ', x) // use template literals instead of string concatenation
if (x > 15) // use curly braces for if statements
{ console.log('x is greater than 15') } // indent the code properly
else {
  console.log('x is less than or equal to 15')
};
console.log('This is a short line') // avoid lines longer than 80 characters
